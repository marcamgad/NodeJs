<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            background-image: url("background3.jpg");
            background-repeat: no-repeat;
            background-size: 100%;
        }

        h1 {
            color: white;
            text-align: center;
            -webkit-text-stroke: 1px blue;
        }

        .container ul {
            font-family: Arial, sans-serif;
            font-size: 18px;
            line-height: 1.6; 
            color: #ffffff; 
        }

        .container ul li {
            margin-bottom: 10px; 
            padding: 5px;
            border: 1px solid rgba(255, 255, 255, 0.2); 
            border-radius: 5px;
            background-color: rgba(0, 0, 0, 0.6); 
        }

        h1 {
            font-size: 36px;
            margin-bottom: 20px;
        }

        .flash-message {
            margin: 15px 0;
            padding: 10px;
            background-color: rgba(255, 0, 0, 0.8);
            color: white;
            font-size: 18px;
            text-align: center;
            border-radius: 5px;
            font-weight: bold;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Your Want-to-Go List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-2">
        <a class="navbar-brand" style="color: rgb(191, 255, 161)">Want-to-Go List</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto"></ul>
            <%- include('includes/search.ejs') %>
        </div>
    </nav>

    <div class="container">
        <% if (errorMessage) { %>
            <div class="flash-message">
                <%= errorMessage %>
            </div>
        <% } %>

        <h1>Your Want-to-Go List</h1>
        <% if (list && list.length > 0) { %>
            <ul>
                <% list.forEach(item => { %>
                    <li>
                        <%= item.destinationId && item.destinationId.name ? item.destinationId.name : 'Unknown Destination' %> - 
                        <%= item.destinationType ? item.destinationType : 'Unknown Type' %>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No destinations added to your list yet!</p>
        <% } %>
    </div>
</body>
</html>
